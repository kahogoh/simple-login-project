import React from 'react'
import ReactDOM from 'react-dom'
import routes from './config/routes'

/**
  * Initialize our routes on to <div id='app'>
  */
ReactDOM.render(routes, document.getElementById('app'))
